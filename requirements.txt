# AutoForge Python dependencies
# Install with: pip install -r requirements.txt

# ── LLM Providers ─────────────────────────────────────────────────────────
groq                        # Groq API — primary LLM provider
google-generativeai         # Google Gemini — second provider fallback
openai                      # OpenAI-compatible client (OpenRouter + GitHub Models)

# ── Environment ───────────────────────────────────────────────────────────
python-dotenv               # Reads .env file for local development
                            # Not needed in Actions — secrets injected via env: